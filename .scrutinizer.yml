build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run
  environment:
    php: 8.2.4

filter:
  excluded_paths:
    - examples/*
    - tests/*
    - vendor/*
    - .github/*
    - .phan/*
